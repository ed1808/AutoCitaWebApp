{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiSettings": {
    "SecretKey": "Esta es una clave secreta de desarrollo para la generación de tokens JWT. No uses esta clave en producción."
  }
}
